import React, { useEffect, useState } from "react"
import { View as V, Text as T, FlatList, Dimensions, TextInput, StyleSheet } from "react-native"

const Products = (props) => {
    return (
        <V>
            <T>Products Screen</T>
        </V>
    )
}

export default Products;